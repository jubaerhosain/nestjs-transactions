---
'@nestjs-transactions/prisma': patch
---

docs(prisma): publish the enhanced README to the npm registry — detailed usage and configuration examples. npm only refreshes the README on a new version, so this patch republishes the package with the updated docs.
